{
    "buildCommand": "poetry -C backend install",
    "builds": [
      {"src": "backend/backend/main.vercel.py", "use": "@vercel/python"}
    ],
    "installCommand": "poetry -C backend install",
    "routes": [
      {"src": "/(.*)", "dest": "backend/backend/main.vercel.py"}
    ]
  }
